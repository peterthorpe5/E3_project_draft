ARIA plant E3 Shiny reporter v0.15.0 validation hand-off
=======================================================

Implemented scope
-----------------

- Added enriched one-row-per-HOG and one-row-per-member All results views.
- Added canonical pre-structure and post-structure ranks while retaining every
  original field from the strongest available HOG-linked ranking relation.
- Added human and Arabidopsis representatives, accessions, entries and raw
  identifiers; membership/species counts and lists; OG/parent-clade links;
  review, mapping, identifier and source summaries; and every raw membership
  field in member detail.
- Preserved exact raw relation browsing and bounded TSV/Excel downloads.
- Added focused validation, query-contract and DuckDB integration tests.

Reported predecessor result
---------------------------

The user reported that the complete R v0.14.0 suite passed before this additive
enriched-HOG results release.

Build-workspace checks on 2026-08-14
------------------------------------

- Edited R files pass delimiter-balance and whitespace checks.
- Edited files end with exactly one newline.
- git diff --check reports no whitespace errors.

Environment limitation
----------------------

This build workspace does not provide R, Rscript, DuckDB or duckplyr. The
complete executable R suite therefore cannot be claimed as run here.

Required release gate
---------------------

    cd E3_shiny_app
    Rscript inst/scripts/check_dependencies.R
    Rscript inst/scripts/run_tests.R

The two test_script_utils.R skips are expected when no --file argument is
available. Any actual test failure blocks release.
