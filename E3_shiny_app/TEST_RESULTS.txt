ARIA plant E3 Shiny reporter v0.12.0 validation hand-off
=======================================================

The user reported that the complete v0.11.1 R suite passed before the v0.12.0
DeepClust/1KP, log-axis and legacy-PDF additions.

Checks completed in this build workspace on 2026-08-13:

- all edited R source and regression-test files pass a quote/comment-aware
  delimiter-balance check;
- the iframe compatibility JavaScript parses successfully;
- both packaged browser PDF scripts parse successfully and the shared PDF
  builder emits a payload beginning with `%PDF` and ending with `%%EOF`;
- Python source and tests compile with `python -m compileall`; and
- edited files pass whitespace/error checks.

Environment limitation:

- this build environment does not contain R/Rscript, DuckDB, pytest, Streamlit,
  Plotly or Kaleido, so it cannot honestly execute either complete app suite.

Run both complete suites before publishing:

    cd e3_python_app
    python -m pip install --editable '.[dev]'
    ./run_tests.sh

    cd ../E3_shiny_app
    Rscript inst/scripts/check_dependencies.R
    Rscript inst/scripts/run_tests.R

The two `test_script_utils.R` skips remain expected when the R session does not
provide a `--file` argument. Any actual failure blocks release.
