ARIA plant E3 Shiny reporter v0.10.6 validation hand-off
=======================================================

Build-environment checks completed on 2026-08-12:

- Delimiters and quoted regions are balanced in all four changed R source and
  regression-test files.
- New R unit tests cover group-choice ordering, highest-ranked last-gate
  defaults, individual filtering, bounded comparison views, selected-group
  summaries and unavailable selections.
- The R UI regression contract checks the searchable selector, group-summary
  output and user-facing search guidance.
- The companion Python v0.7.6 suite passes all 94 tests with 95% branch-aware
  coverage, including headless switching to a different evolutionary group.

Environment limitation:

- This build environment does not contain R/Rscript or the native Shiny package
  stack, so the complete R testthat suite could not be executed here.
- The preceding v0.10.4 hotfix suite passed on the target Mac. Re-run the full
  suite after applying v0.10.6:

    Rscript E3_shiny_app/inst/scripts/check_dependencies.R
    Rscript E3_shiny_app/inst/scripts/run_tests.R

The two `test_script_utils.R` skips remain expected when the R session does not
provide a `--file` argument.
