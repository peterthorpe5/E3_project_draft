ARIA plant E3 Python reporter v0.13.0 validation hand-off
========================================================

Implemented scope
-----------------

- Added the independent structural-review top-200-to-500 HOG shortlist.
- Excluded structural-stage fields and tie-breaks from that shortlist.
- Enriched both Threshold explorer outputs with substantially more source and
  root-HOG membership information.
- Added the exact-authority-aware, searchable and downloadable E3 seed
  catalogue with a scientifically labelled compatibility fallback.
- Added focused unit, query-contract, fallback and DuckDB integration tests.

Build-workspace checks on 2026-08-14
------------------------------------

- All Python source and tests compile successfully.
- Full-project pycodestyle passes with the configured 100-character limit.
- Full-project pydocstyle passes.
- Application launch scripts pass `bash -n`.
- Edited files pass whitespace and terminal-newline checks.

Environment limitation
----------------------

This build workspace does not contain a usable DuckDB/pytest/Streamlit test
environment. The dependency-backed functional suite and recalculated branch
coverage therefore cannot honestly be claimed as run here.

Required release gate
---------------------

    cd e3_python_app
    python -m pip install --editable '.[dev]'
    ./run_tests.sh

All tests and linters must pass and total branch coverage must be at least 95%.
Any actual failure blocks release.
