ARIA plant E3 Python reporter v0.12.0 validation hand-off
========================================================

Implemented scope
-----------------

- Added enriched one-row-per-HOG and one-row-per-member All results views.
- Added canonical pre-structure and post-structure ranks while retaining every
  original field from the strongest available HOG-linked ranking relation.
- Added human and Arabidopsis representatives, accessions, entries and raw
  identifiers; membership/species counts and lists; OG/parent-clade links;
  review, mapping, identifier and source summaries; and every raw membership
  field in member detail.
- Preserved raw relation browsing and bounded TSV/Excel downloads.
- Added focused defensive and SQL integration tests for duplicate-ranked,
  membership-only and ranking-only HOGs.

Build-workspace checks on 2026-08-14
------------------------------------

- All edited Python source and tests compile successfully.
- No new or edited Python line exceeds 100 characters.
- Edited files end with exactly one newline.
- git diff --check reports no whitespace errors.

Environment limitation
----------------------

The build workspace does not contain DuckDB, pytest or the other application
dependencies, and the offline package cache does not contain DuckDB. The full
executable Python suite therefore cannot be claimed as run here.

Required release gate
---------------------

    cd e3_python_app
    python -m pip install --editable '.[dev]'
    ./run_tests.sh

Any actual test or style failure blocks release.
