ARIA plant E3 Python reporter v0.9.2 validation hand-off
=======================================================

User-reported v0.9.1 result on 2026-08-13:

- 111 tests passed;
- one headless Streamlit test failed because its synthetic fixture did not
  supply the default hierarchical-membership relation; and
- eight warnings were reported, including non-failing Plotly/Kaleido
  deprecation warnings.

Version 0.9.2 repairs only the test fixture and scopes nested-widget assertions
to the relevant Streamlit tabs. Application logic and scientific results are
unchanged.

Checks completed in this build workspace:

- Python source and tests compile with `python -m compileall`;
- changed Python files meet the 100-character line-length target;
- obsolete fixture structure is absent;
- both production-like OrthoFinder membership levels and the seeded-member
  relation are present in the fixture;
- both corrected selector labels remain asserted; and
- repository whitespace/error checks pass.

Environment limitation:

- this build environment does not contain DuckDB, pytest or Streamlit, so it
  cannot execute the complete Python suite.

Required confirmation:

    cd e3_python_app
    python -m pip install --editable '.[dev]'
    ./run_tests.sh

Expected result: 112 passed tests, subject only to non-failing dependency
warnings. Any actual failure blocks release.
