# Frozen inherited scripts

These five files are exact, checksum-protected copies of the inherited scripts
recovered from `Drost_lab_ligandability/src`.

They are retained for provenance and regression comparison only. They are not
production entry points and must not be edited in this directory.

`LEGACY_FILE_CHECKSUMS.tsv` is enforced by the release-contract test.

The recovered `fpocket_inherited_environment.yaml` is also retained as
provenance. It records FPocket 4.2.2 and the original personal environment
prefix. It is evidence, not a portable environment file. Its SHA-256 digest is
`387e6e4391ca39c518f1196650c50ec7a81cd615f04ef0443b0ea28c35404a41`.
